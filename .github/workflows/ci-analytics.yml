name: Analytics CI

on:
  push:
    paths:
      - 'services/analytics/**'
      - '.github/workflows/ci-analytics.yml'
    branches: [ master ]
  pull_request:
    paths:
      - 'services/analytics/**'
      - '.github/workflows/ci-analytics.yml'
    branches: [ master ]

jobs:
  analytics-tests:
    name: Run FastAPI Analytics Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/analytics
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('services/analytics/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: pip install -e .[test]

      - name: Check import order with isort
        run: isort . --check --diff

      - name: Lint with ruff
        run: ruff check .

      - name: Type check with mypy
        run: mypy app.py tests/test_app.py

      - name: Run pytest
        run: pytest -q
